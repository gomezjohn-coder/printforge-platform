apiVersion: v2
name: order-service
description: PrintForge Order Service — handles checkout, payment orchestration, and the order lifecycle. Owner of the 99.9% Checkout Availability SLO.
type: application
version: 0.1.0
appVersion: "1.0.0"
maintainers:
  - name: PrintForge Platform Team
    email: platform@printforge.io
dependencies:
  - name: common
    version: "0.1.0"
    repository: "file://../common"
